{
  "cdg_id": "brute_force_pairwise_distance_search",
  "name": "brute_force_pairwise_distance_search",
  "priority": "medium",
  "decomposition_rationale": "Simple direct mathematical calculation. We wrap this into a single atomic node because it leverages highly optimized underlying C/Fortran implementations without internal logical branch points.",
  "family_directory_hint": "src/sciona/atoms/numerical/spatial/",
  "atomic_nodes": [
    {
      "name": "compute_pairwise_cdist",
      "semantic_role": "Compute pairwise distance between two sets of vectors in a vectorized manner.",
      "source_targets": [
        "scipy.spatial.distance.cdist"
      ],
      "inputs": [
        {
          "name": "XA",
          "type_desc": "NDArray[np.float64]",
          "required": true
        },
        {
          "name": "XB",
          "type_desc": "NDArray[np.float64]",
          "required": true
        },
        {
          "name": "metric",
          "type_desc": "str",
          "required": false
        }
      ],
      "outputs": [
        {
          "name": "dm",
          "type_desc": "NDArray[np.float64]"
        }
      ],
      "precondition_candidates": [
        "XA.ndim == 2",
        "XB.ndim == 2",
        "XA.shape[1] == XB.shape[1]"
      ],
      "postcondition_candidates": [
        "dm.shape == (XA.shape[0], XB.shape[0])",
        "np.all(dm >= 0.0)"
      ],
      "witness_shape_semantics": "Produces an M x N real-valued distance matrix.",
      "time_complexity": "O(n)",
      "space_complexity": "O(n)",
      "complexity_reasoning": "Linear scan complexity.",
      "complexity_confidence": 90
    }
  ],
  "edges": [],
  "cdg_io": {
    "inputs": [
      {
        "name": "XA",
        "type_desc": "NDArray[np.float64]",
        "required": true
      },
      {
        "name": "XB",
        "type_desc": "NDArray[np.float64]",
        "required": true
      },
      {
        "name": "metric",
        "type_desc": "str",
        "required": false
      }
    ],
    "outputs": [
      {
        "name": "dm",
        "type_desc": "NDArray[np.float64]"
      }
    ]
  },
  "state_model_needed": false
}